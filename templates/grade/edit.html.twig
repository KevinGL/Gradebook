{% extends 'base.html.twig' %}

{% block title %}Mettre à jour cette note{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-10 bg-white p-8 rounded-xl shadow">
        <h1 class="text-2xl font-semibold text-gray-800 mb-6">Mettre à jour cette note</h1>

        {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

            <div>
                {{ form_label(form.value, 'Note', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.value, {
                    'attr': {
                        'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500'
                    }
                }) }}
            </div>

            <div>
                {{ form_label(form.date, 'Date', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.date, {
                    'attr': {
                        'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500'
                    }
                }) }}
            </div>

            <div class="flex justify-end space-x-4">
                <a href="{{ url('view_student', {'id': studentID, 'subjectID': subjectID, 'trimester': trimester}) }}"
                class="px-5 py-2 bg-gray-200 text-gray-700 rounded-lg shadow hover:bg-gray-300 transition">
                    Annuler
                </a>
                <button type="submit"
                        class="px-6 py-2 bg-indigo-600 text-white font-medium rounded-lg shadow hover:bg-indigo-700 focus:ring-2 focus:ring-indigo-500 transition">
                    💾 Mettre à jour
                </button>
            </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
