<nav class="bg-indigo-600 text-white shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            
            <div class="flex-shrink-0 text-lg font-bold">
                <a href="{{ path('app_grades') }}">📘 Gradebook</a>
            </div>

            <div class="flex space-x-6">
                <a href="{{ path('app_grades') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md text-sm font-medium">Accueil</a>

                <a href="{{ path('app_logout') }}" class="hover:bg-red-500 px-3 py-2 rounded-md text-sm font-medium">Déconnexion</a>
            </div>
        </div>
    </div>
</nav>