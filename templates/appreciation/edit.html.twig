{% extends 'base.html.twig' %}

{% block title %}Modifier l'appréciation{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-10 bg-white p-8 rounded-xl shadow">
        <h1 class="text-2xl font-semibold text-gray-800 mb-6">Modifier l'appréciation</h1>

        {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

            <div>
                {{ form_label(form.text, 'Appréciation', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.text,
                    {
                        'attr':
                        {
                            'rows': 6,
                            'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 resize-none'
                        }
                    })
                }}
            </div>

            <div class="flex justify-end space-x-4">
                <a href="{{ app.request.headers.get('referer') ?? path('app_home') }}"
                    class="px-5 py-2 bg-gray-200 text-gray-700 rounded-lg shadow hover:bg-gray-300 transition">
                    Annuler
                </a>
                <button type="submit"
                        class="px-6 py-2 bg-indigo-600 text-white font-medium rounded-lg shadow hover:bg-indigo-700 focus:ring-2 focus:ring-indigo-500 transition">
                    💾 Enregistrer
                </button>
            </div>

        {{ form_end(form, { 'render_rest': false }) }}
    </div>
{% endblock %}
